;;; Package --- Reactf Setup
;;; Commentary:
;;; Code:

(use-package recentf
  :defer 1
  :config
  (recentf-mode 1)
  (setq recentf-max-menu-items 25)
  )

(provide 'recentf-setup)
;;; recentf-setup.el ends here
